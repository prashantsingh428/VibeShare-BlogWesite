<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('./partials/head') %>
</head>

<body
    class="bg-gradient-to-br from-indigo-100 via-purple-100 to-pink-100 min-h-screen flex flex-col text-slate-800 relative overflow-hidden">

    <%- include('./partials/navbar') %>

        <div class="flex-grow flex flex-col items-center justify-start pt-32 w-full px-4 pb-20">

            <!-- Decorative Background Blobs -->
            <div
                class="absolute top-1/2 left-1/4 w-72 h-72 bg-purple-300 rounded-full mix-blend-multiply filter blur-3xl opacity-30 animate-blob">
            </div>
            <div
                class="absolute top-1/2 right-1/4 w-72 h-72 bg-indigo-300 rounded-full mix-blend-multiply filter blur-3xl opacity-30 animate-blob animation-delay-2000">
            </div>
            <div
                class="absolute bottom-1/4 left-1/2 w-72 h-72 bg-pink-300 rounded-full mix-blend-multiply filter blur-3xl opacity-30 animate-blob animation-delay-4000">
            </div>

            <!-- Login Card -->
            <div
                class="w-full max-w-md glass-card rounded-3xl shadow-2xl p-8 relative z-10 transition-all duration-300 hover:shadow-3xl">

                <!-- Header -->
                <div class="mb-8 text-center">
                    <div
                        class="w-16 h-16 rounded-2xl bg-gradient-to-tr from-indigo-500 to-purple-500 flex items-center justify-center text-white text-3xl font-bold shadow-lg mx-auto mb-4">
                        <i class="ri-pulse-line"></i>
                    </div>
                    <h2 class="text-3xl font-bold text-slate-800">
                        Welcome Back 👋
                    </h2>
                    <p class="text-slate-500 mt-2">
                        Enter your credentials to access your account.
                    </p>
                </div>

                <!-- Error Alert -->
                <% if (typeof error !=='undefined' && error) { %>
                    <div
                        class="bg-red-500/10 border border-red-500/50 text-red-600 px-4 py-3 rounded-xl mb-6 flex items-center gap-2 animate-pulse">
                        <i class="ri-error-warning-line text-xl"></i>
                        <span class="font-medium text-sm">
                            <%= error %>
                        </span>
                    </div>
                    <% } %>

                        <!-- Form -->
                        <form action="/login" method="post" class="space-y-6">

                            <!-- Email -->
                            <div>
                                <label
                                    class="block text-xs font-semibold text-slate-500 uppercase mb-1 ml-1">Email</label>
                                <div class="relative">
                                    <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none">
                                        <i class="ri-mail-line text-slate-400"></i>
                                    </div>
                                    <input type="email" name="email" placeholder="you@example.com" required
                                        class="w-full pl-11 pr-4 py-3 rounded-xl bg-white/50 border border-slate-200 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:bg-white transition text-slate-700">
                                </div>
                            </div>

                            <!-- Password -->
                            <div>
                                <div class="flex items-center justify-between mb-1 ml-1">
                                    <label class="block text-xs font-semibold text-slate-500 uppercase">Password</label>
                                    <a href="#"
                                        class="text-xs text-indigo-600 hover:text-indigo-800 font-medium hover:underline">Forgot?</a>
                                </div>
                                <div class="relative">
                                    <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none">
                                        <i class="ri-lock-line text-slate-400"></i>
                                    </div>
                                    <input type="password" name="password" placeholder="••••••••" required
                                        class="w-full pl-11 pr-4 py-3 rounded-xl bg-white/50 border border-slate-200 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:bg-white transition text-slate-700">
                                </div>
                            </div>

                            <!-- Button -->
                            <button type="submit"
                                class="w-full py-3.5 rounded-xl bg-gradient-to-r from-indigo-600 to-purple-600 text-white font-bold hover:shadow-lg hover:from-indigo-700 hover:to-purple-700 transform hover:-translate-y-0.5 transition-all flex items-center justify-center gap-2">
                                <span>Log In</span>
                                <i class="ri-arrow-right-line"></i>
                            </button>

                        </form>

                        <div class="mt-8 pt-6 border-t border-slate-200/60 text-center text-sm text-slate-500">
                            Don’t have an account?
                            <a href="/#register"
                                class="text-indigo-600 hover:text-indigo-800 font-bold hover:underline transition">
                                Create one
                            </a>
                        </div>

            </div>
        </div>

</body>

</html>

</html>